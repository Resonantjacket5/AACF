<%= form_for(@carousel_item) do |f| %>
  <% if @carousel_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@carousel_item.errors.count, "error") %> prohibited this carousel_item from being saved:</h2>

      <ul>
      <% @carousel_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :img_url %><br>
    <%= f.text_area :img_url %>
  </div>
    <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :disable %><br>
    <%= f.check_box :disable %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
