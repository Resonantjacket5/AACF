
<h3> Seniors Present: <%= @Seniors_present.count%> </h3>
  <% @Seniors_present.each do |student| %>
    <%= link_to student.name, destroy_attendance_path(event_id: @event.id, student_id: student.id), method: :delete, remote: true, class: "btn btn-success #{student.id}" %> 
  <% end %>