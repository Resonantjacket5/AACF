$("#refresh_seniors").html("<%= j (render 'seniors_present') %>");
$("#refresh_juniors").html("<%= j (render 'juniors_present') %>");
$("#refresh_sophomores").html("<%= j (render 'sophomores_present') %>");
$("#refresh_freshmen").html("<%= j (render 'freshmen_present') %>");
$("#refresh_others").html("<%= j (render 'others_present') %>");