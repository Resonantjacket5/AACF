  <% @Juniors.each do |student| %>
  
    <% if @students.where(id: student.id).blank? %>
  
      <%= link_to student.name, create_attendance_path(event_id: @event.id, student_id: student.id), method: :post, remote: true, class: "btn btn-primary #{student.id}" %>
  <% end %>

  <% end %>