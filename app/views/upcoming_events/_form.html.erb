<%= form_for(@upcoming_event) do |f| %>
  <% if @upcoming_event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@upcoming_event.errors.count, "error") %> prohibited this upcoming_event from being saved:</h2>

      <ul>
      <% @upcoming_event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_area :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :location %><br>
    <%= f.text_area :location %>
  </div>
  <div class="field">
    <%= f.label :event_time %><br>
    <%= f.datetime_select :event_time %>
  </div>
  <div class="field">
    <%= f.label :img_url %><br>
    <%= f.text_area :img_url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
