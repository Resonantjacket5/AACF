<%= form_for(@large_group) do |f| %>
  <% if @large_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@large_group.errors.count, "error") %> prohibited this large_group from being saved:</h2>

      <ul>
      <% @large_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :weekday %><br>
    <%= f.text_area :weekday %>
  </div>
  <div class="field">
    <%= f.label :location %><br>
    <%= f.text_area :location %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :event_time %><br>
    <%= f.time_select :event_time %>
  </div>
  <div class="field">
    <%= f.label :img_url %><br>
    <%= f.text_area :img_url %>
  </div>
  <div class="actions">
    <%= f.submit class: "btn btn-warning" %>
  </div>
<% end %>
