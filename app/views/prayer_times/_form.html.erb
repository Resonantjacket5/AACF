<%= form_for(@prayer_time) do |f| %>
  <% if @prayer_time.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@prayer_time.errors.count, "error") %> prohibited this prayer_time from being saved:</h2>

      <ul>
      <% @prayer_time.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :location %><br>
    <%= f.text_area :location %>
  </div>
  <div class="field">
    <%= f.label :event_time %><br>
    <%= f.time_select :event_time %>
  </div>
  <div class="field">
    <%= f.label :weekday %><br>
    <%= f.text_area :weekday %>
  </div>
  <div class="field">
    <%= f.label :img_url %><br>
    <%= f.text_area :img_url %>
  </div>
  <div class="field">
    <%= f.label :image_active %><br>
    <%= f.check_box :image_active %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
